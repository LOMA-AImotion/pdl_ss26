# data/

This directory is used to store datasets downloaded during the course notebooks.

It is intentionally excluded from version control (see `.gitignore`) to avoid committing large binary files.

Datasets will be downloaded automatically when you run the relevant notebook cells (typically via `torchvision.datasets` or a helper in `utils/data.py`).
